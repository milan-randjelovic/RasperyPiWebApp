@using WebPortal.Models.Switches
@model IEnumerable<ISwitch>

@{
    ViewBag.Title = "Switches";
    int i = 1;
}

<h2>@ViewBag.Title</h2>

<div class="panel">
    @Html.ActionLink("Create", "Create", "Switches", null, new { @class = "btn btn-success" })
</div>

<table class="table table-responsible table-hover table-bordered">
    <thead>
        <tr>
            <th>
                #
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SwitchType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RaspberryPinNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.State)
            </th>
            <th>
                Actions
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @i.ToString()
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SwitchType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RaspberryPinNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.State)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { item.Id }, new { @class = "btn btn-warning" }) |
                @Html.ActionLink("Details", "Details", new { item.Id }, new { @class = "btn btn-info" }) |
                @Html.ActionLink("Delete", "Delete", new { item.Id }, new { @class = "btn btn-danger" })
            </td>
        </tr>
            i++;
        }
    </tbody>
</table>
